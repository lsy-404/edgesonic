# Findings

## Decision

The six pending files in `矩尺镜海·蚀刻于此媒介A` are a bounded safe cohort for completing the existing A edition. The directory has an explicit A suffix, its six pending rows occupy numbered positions 1, 2, 5–8, and positions 3–4 are already linked to the existing `矩尺镜海·蚀刻于此媒介A` album. The sibling B directory has its own sixteen-file sequence and its own cover; overlapping names such as `信号心桥`, `答案不唯一`, `Sublimity`, and `系统万象` confirm that A/B are distinct editions. The proposal touches only six A entries and retains the two existing anchors.

`梦境电台` is held. The 24 pending files are mixed with four already assigned audio masters: two in an album named `wav`, and two in `Unknown Album`. They span positions 1–29 with gaps caused by those four already-assigned rows, so the pending cohort alone is not a complete album identity.

The B directory is an independent 16-track edition: `01 清澜若雪-Narrator Version-`, `02 信号心桥`, `03 深秋之霜`, `04 卡珊德拉症候群`, `05 Sublimity`, `06 答案不唯一`, `07 答案不唯一-Mobile Version-`, `08 Cassandra`, `09 进化算法`, `10 致邀请老用户`, `11 献给黑色圣母的檄文`, `12 月白非白`, `13 处决一束白月光`, `14 沉没海妖之礁`, `15 刀风刃雨`, `16 系统万象`. The A edition has eight numbered positions with two established anchors and two bonus tracks; the B list has a different sequence and version-specific tracks. Keep A and B separate as two albums; a later presentation-layer release should place them in a shared display group. The shared root has separate `coverA.jpg` and `coverB.jpg` sidecars. No audio or sidecar objects are modified by the candidate. PCM equivalence was not asserted from titles or etags.

## Fresh primary evidence

The A directory primary SELECT returned eight WAV instances under exact parent `se-698bb4fd7be549aaa70de23aa64ae9a1`. Pending masters are positions 1, 2, 5, 6, 7, 8. Existing album `al-86f72c214f` is named `矩尺镜海·蚀刻于此媒介A`; its same-artist anchor masters are position 3 `系统万象` and position 4 `致邀请老用户`. All eight rows have physical `objects/*.wav` keys and source `r2-local`. The A target album currently has 2 masters, stored duration 0, and size 70,560,088; summing its masters gives duration 400 and size 70,560,088. The six pending files sum to 1,140 seconds and 201,096,264 bytes. A successful archive should produce target caches of 8 / 1,540 seconds / 271,656,352 bytes with tracks 1–8 unique. Initial pending preflight was 581 / 129,701 / 22,611,646,437. After the repository operator archived another cohort, the refreshed primary cache became 555 / 123,211 / 21,466,454,141 and still agrees with live aggregates. The updated candidate predicts pending 549 / 122,071 / 21,265,357,877.

The album and A cohort query results were served by D1 primary with `rows_written=0` and `changed_db=false`. This audit does not write production.

## Guard requirements

The SQL candidate compares each of six source rows using NULL-safe checks, including master identity/title/duration/untagged album and track state, instance source/URI/suffix/duration/size/missing/tag scan/object/etag, entry id/source/parent/path/display/kind/object/companion, and object physical key/suffix/size/legacy key/etag. It also guards both existing A anchor rows and exact target album and pending cache state. The CUE and audio object rows are never deleted or changed.

## Verification

Fresh primary preflight after the other cohort archive returned all six expected source rows and the refreshed target/cache aggregates, with `served_by_primary=true`, `rows_written=0`, and `changed_db=false`. The local Wrangler rehearsal passed all three scenes: successful assignment/recalculation; stale physical-key change rejected without candidate writes; and a forced final-statement CHECK failure. The late scene compares canonical before/after JSON fingerprints covering target and pending albums, all their masters, linked instances, source entries, storage objects, and the queue; the fingerprint was identical, including the deliberate marker.

An independent local Wrangler probe inserts a valid marker followed by an invalid queue status and confirms that the marker is absent after `--file` exits nonzero. The repository operator separately confirmed rollback through the remote `wrangler d1 execute --remote --file` path. An earlier concern came from an older candidate whose late guard had not actually failed; it was not evidence of partial commit. This audit itself has made no production writes.

## A anchor correction and B edition follow-up

After A was archived, two established A anchors still had `disc=NULL`, while the six archived masters have `disc=1`. A fresh primary SELECT-only read confirmed the two exact anchors (`sm-5e95a72c01` / `系统万象`, position 3; `sm-c1be4c1e30` / `致邀请老用户`, position 4), their original source entries, and their object physical keys. `prepare_ruler_a_anchor_disc.sql` repairs only these two `disc` fields. Its guard compares the complete source/master identity and requires the current production pending cache of 539 / 120034 / 20877833365 and the A album cache 8 / 1540 / 271656352. It does not touch the album cache or any audio/storage references.

B remains an independent 16-track WAV edition. The refreshed primary query returned all 16 original masters, instances, entries, and objects in the B directory, with 3230 seconds and 569772704 bytes. The SELECT-only API response was served by primary, wrote zero rows, and reported no database change. The current pending cache and live aggregate agree at 539 / 120034 / 20877833365; A is 8 / 1540 / 271656352. There is no B album or A/B display group. `prepare_ruler_b_album_and_group.sql` now guards these current values, the corrected A disc sequence, every B source field including object physical key, and absence of the target B album/group. If run after the anchor correction, it expects B 16 / 3230 / 569772704 and pending 523 / 116804 / 20308060661. It creates distinct A and B albums and a two-member display group only in the B archive transaction.

The local test fixture models the post-A, pre-B production state. The Wrangler local run passed: anchor repair plus B/group success; B source physical-key drift rejected with the pre/post database fingerprint unchanged; and an intentionally invalid final write caused a nonzero exit with the full fingerprint unchanged. The fingerprint includes the A/pending/B albums and masters, linked instances/entries/objects, both display-group tables, and the queue.

## Explicit duplicate example: `平四1-6

A primary SELECT found one pending master beneath that root: `sm-upload-1c5e734b-599`, title `10  -最后的歌`, path `平四1-6/平行四界5/10  -最后的歌.wav`, duration 259 seconds, 45,619,436 bytes, WAV object `obj_790adcf9317cb20f`, etag `4712159da2cb9e921adf03fe6016734c`. An existing assigned master in album `平行四界Quadimension 5` is track 10 `最后的歌`, also 259 seconds, in FLAC, 23,568,474 bytes, with different object and etag. This metadata match warranted a PCM comparison. The exact decoded PCM check below shows the WAV and FLAC are different recordings/masters, so both are retained; the follow-up proposal gives the WAV a separate alternate-edition album. The R2 GETs were read-only.

### PCM verification update

Both exact R2 objects were downloaded with remote Wrangler GET to the ignored local rehearsal directory and left unchanged in R2. FFprobe reports both audio streams as 44.1 kHz, stereo, 16-bit, 11,404,848 samples per channel (258.613333 seconds). The FLAC also has an attached 700×700 cover and comments for album `平行四界Quadimension 5`, title `最后的歌`, and track 10; the WAV has no audio-container tags. The decoded s32le PCM streams are each 91,238,784 bytes, but differ by SHA-256: WAV `D1068BF860BB8CBFD5A117297D1C482B3C230A64D99896D94E16E0C1571ED48D`; FLAC `9756AD2B14DACBB8BC0AAEE21008A122FB45F28FAE444FDEEA65AAA0E241BD64`. Raw object SHA-256 values are WAV `612E0B8C0DCFB2E78C54E718954F8638630DE324B2A624771F3A9ECE05D57853` and FLAC `A07825D84D9C3712B6D3FC2E9486FC9BC886ADEC781BEF48CB3D594F847B268D`.

This is not a byte-equivalent transcode. A fresh primary reference scan found exactly one instance and one logical entry for the pending WAV, with no annotations, playlist rows, bookmarks, share entries, scrape rows, or saved queue references; all checks were SELECT-only, primary-served, and wrote zero rows. Preserve both objects and the existing track-10 master. Per repository operator direction, prepare this separately from the A/B batch as a one-track WAV alternate-edition album, display-grouped with the existing Quadimension 5 album; do not reuse the existing track slot or retire the FLAC.

## Separate Quadimension 5 WAV alternate edition

After the A/B production postflight, the current primary pending cache is 523 / 116804 / 20308060661 and still matches the live aggregate. The WAV candidate and the existing FLAC track 10 remain unchanged and have one physical object reference each. The base album is `al-ac89e8964d`, `平行四界Quadimension 5`, 2016, 10 tracks / 2691 seconds / 457556360 bytes, sort name `平行四界quadimension 5`, cover key `covers/al-ac89e8964d`; it is not already in a display group. The candidate preflight is a fresh primary SELECT-only read with two exact master/source rows, no matching display group or alternate album, rows_written=0, changed_db=false.

The separate guarded SQL candidate creates `al-q5-wav-alternate` as a one-track alternate album, copies the existing edition's year and cover reference, normalizes the master metadata to the FLAC's title and artist/album-artist IDs, and places it at track 10/disc 1. It preserves the existing FLAC master and both audio objects. It adds `ag-q5-edition-variants` with the full FLAC album first and the WAV alternate second. Its exact source guard covers the pending WAV master, instance, entry, object physical key/content type/size/etag, the existing FLAC track 10 and its source object, base album metadata, absence of alternate album/group/membership, and the current pending cache. Expected post-archive caches are alternate album 1 / 259 / 45619436 and pending 522 / 116545 / 20262441225.

The isolated Wrangler local rehearsal passed all three scenes: successful alternate album/group creation while retaining the FLAC master; changed WAV `physical_key` rejected with an unchanged before/after full database fingerprint; and a forced final CHECK failure with the same full fingerprint, including album, master, instance, entry, object, group, and queue rows. Production remains unchanged by this candidate.
