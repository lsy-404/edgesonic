# Progress

- 复现并定位搜索结果显示分组遗漏。搜索请求现在同步取得分组摘要，搜索专辑卡复用现有折叠与版次详情逻辑。
- Web 类型检查及已有分组折叠测试通过；首轮已合并、推送、部署。
- 线上验收发现搜索卡点击后的版次详情被搜索条件隐藏；追加修复后已再次合并、推送和部署。
- `Lost In Tianyi` 搜索现在只显示一张“2 个版本”卡，点击后能选择两张各九轨原始专辑。线上版本接口与构建资产均显示 `1.4.0-dev.4a8b2ab`；部署回执保留 `SSO_MODE=required` 与 `Voidcarve Access`。
