# 实施计划

## 范围

1. [x] 调整 OIDC 身份映射，使既有用户通过预置映射继续使用原账户。
2. [x] 强制 SSO 时自动启动登录，并阻止所有旧 Web 凭据入口。
3. [ ] 增加生产配置与幂等数据库迁移。
4. [ ] 验证登录、刷新、退出和失败恢复的完整浏览器链路。

## 验收

- [x] 已映射用户不会创建新的合成账户。
- [x] 强制模式下访问登录页自动进入身份中心。
- [x] 回调失败可显示错误，不形成自动跳转循环。
- 旧密码、注册、重置接口在强制模式下一律拒绝。
- 生产实例仅保留 SSO 登录入口。
