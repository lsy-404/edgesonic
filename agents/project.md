# EdgeSonic 项目索引

> 最后更新：2026-09-26

## 项目目标

提供基于 Cloudflare Workers 的 Subsonic 兼容音乐流媒体服务。

## 技术栈

TypeScript、Vue、Cloudflare Workers、D1 与 R2。

## 模块结构

`worker/` 包含服务端与数据库迁移；`web/` 包含前端；`scripts/` 与 `tools/` 包含运维和审计工具。
