# 操作记录

- 已创建隔离 worktree，并确认该分支从 `main` 开始。
- 已核对稳定对象迁移和上传路由，开始实现对象先于实例的写入顺序。
- 已将稳定对象的 upsert 放入新实例的 D1 batch，并在覆盖更新前单独持久化对象；随后仍由现有注册函数写逻辑目录和实例关联。
- 已扩展上传冲突测试，使模拟数据库在对象尚未创建时拒绝实例插入或更新。
- 首次验证因独立 worktree 未安装依赖而未启动，正在安装 lockfile 对应依赖后重试。
- 已安装锁定依赖；Worker 类型检查通过。修正测试替身的 D1 batch 执行语义后重新运行回归测试。
- 上传回归测试和最终 Worker 类型检查均已通过，准备提交。
