# Find-Zero multi-disc backfill audit

The candidate set is limited to the 25 historical pending-upload masters previously excluded for Find-Zero disc context. This task will retain all masters, instances, storage objects, and R2 bytes. It can only change D1 album and disc metadata after fresh primary verification and a local rollback rehearsal.

[Primary scope query failed] -> The first read-only query selected a non-existent legacy album artist column -> corrected the scope to the current album schema before retrying. No query wrote data.

Fresh primary reads returned 25 pending WAV masters: 14 below `CD 1 人声碟` and 11 below `CD 2 伴奏碟`. Each has exactly one original, scanned, present `r2-local` WAV instance, exactly one stable object with the expected immutable physical key, and exactly one matching file entry. The CD1 sequence is the same 14 titles and numbers as the existing 14-track FLAC `Find-Zero` album (`al-0494f8ac9c`); CD2 intentionally lacks numbers 1, 7, and 12 and is a separate accompaniment disc. No candidate has a playlist or song annotation reference.

The proposed D1-only operation creates a separate 25-track `Find-Zero` WAV album, assigns disc 1 or 2 from the immutable disc-folder identity, assigns source filename track numbers, and places it with the existing FLAC edition in a new display group. It never relinks instances to the FLAC masters and does not mutate storage entries, storage objects, or R2 bytes. The scope leaves the two cover images, 13 lyrics, nine lyric-book images, and all existing FLAC entries untouched.

The new edition is named `Find-Zero (WAV)` with `sort_name='find-zero-wav'`; its year and cover remain unset, while its genre retains the existing FLAC album's observed `未知流派` value. CD2 uses its real source-file numbers 2, 3, 4, 5, 6, 8, 9, 10, 11, 13, and 14. The missing 1, 7, and 12 are not renumbered because no corresponding accompaniment files exist in the physical tree.

`rehearse.py` passed an isolated SQLite apply/rollback cycle: all 25 rows moved and restored, 25 objects and entries remained present, a stale master title rejected the apply guard, and an annotation added after apply rejected rollback. The final production preflight was primary-served and read-only: all 25 master, instance, object, and entry predicates matched; target album and memberships were absent; the existing 14-track FLAC album was present; playlist and annotation reference counts were zero.

Production R2 PCM verification fetched all 14 CD1 WAV and 14 existing FLAC objects read-only. FFmpeg decoded each file to interleaved two-channel signed 32-bit little-endian PCM and `pcm_receipt.json` records byte count, sample-frame count, and SHA-256 for each side. All 14 pairs differ. Every pair has a different decoded frame count; for example, track 1 is 4,104,828 WAV frames (93.08 seconds) versus 4,193,028 FLAC frames (95.08 seconds). The WAV and FLAC masters are distinct recordings or edits, so a tag merge, master replacement, FLAC logical retirement, or R2 retirement is not safe. Keep both albums and use only the display group proposed by `apply.sql` after its guarded preflight is rerun immediately before any approved production execution.

[Wrangler local file rejected candidate] -> The initial apply/rollback scripts create TEMP tables for scope and assertions. D1 local through Wrangler rejects that pattern with SQLITE_AUTH, so the existing SQLite-only rehearsal does not validate the real execution path. Replace it with per-statement CTE scopes and the repository's CHECK-constrained work_queue fail-fast guard; execute the scripts through the real local Wrangler CLI.

[Wrangler D1 local verification] -> `wrangler d1 execute --local --file` rejected the former TEMP-table candidate with SQLITE_AUTH. Replaced TEMP tables and manual transaction syntax with repeated `WITH c(...) AS (VALUES ...)` snapshots, a `work_queue.status` CHECK failure for stale scopes, and `changes()` assertions inside Wrangler's file batch. A local Wrangler rehearsal now passes apply, full rollback, stale-title refusal, and late-annotation rollback refusal; post-failure reads confirm the batch made no partial changes.

[Fresh primary preflight] -> The exact 25-row source snapshot still matches on the primary. The live read-only query returned 25 candidates, 25 exact source master/instance/object/entry/path/title matches, zero target album or display group, one existing 14-track FLAC album, zero existing memberships, zero song references, and zero child entries. D1 metadata reports `served_by_primary=true`, `rows_written=0`, and `changed_db=false`. No production write was issued.

[Release year follow-up] -> The proposed year 2022 had no verified source. A fresh primary read of the existing 14-track `Find-Zero` album returned `year=NULL`, so the WAV edition now also leaves year unset. The local fixture mirrors the NULL source year, and the Wrangler rehearsal explicitly asserts `year IS NULL`. The primary scope preflight still matches all 25 exact source rows and reports zero target album/group and zero writes.

[FLAC album aggregate repair] -> A primary-served read-only snapshot returned 14 FLAC masters and 14 instances. Master durations and instance durations both sum to 3173 seconds; instance sizes sum to 411082170 bytes. The existing album cache is `song_count=14`, `duration=0`, and `size=411082170`. The guarded apply requires this exact old cache and both duration/count/size aggregates, then recalculates `pending-uploads`, the WAV album, and the FLAC album from master durations and instance sizes. The corrected 3173-second FLAC duration remains corrected if the WAV grouping is rolled back.

[Wrangler aggregate rehearsal] -> The real local Wrangler D1 fixture uses the 14 primary-derived FLAC master/instance rows and all 25 WAV candidates. The success scenario confirms FLAC duration changes from 0 to 3173, the cache matches the master sum and instance sum, and its size remains 411082170. WAV and pending cache totals match their source sums. Apply/rollback, stale-scope refusal, and late-reference rollback refusal all pass.

[Fresh primary aggregate preflight] -> Primary D1 returned 25 candidate/25 exact rows; the expected old FLAC cache matches once, with 14 masters/14 instances, both duration sums 3173, and size sum 411082170. The target/group/memberships/references/children remain absent. Metadata reports `served_by_primary=true`, `rows_written=0`, and `changed_db=false`.
